# \begin{align*}
#   \alpha &= ( \beta + \eta ) \\ \gamma &= [ \delta - \nu ] \\
#   \alpha &= ( \nu + \nabla ) \\ \xi &= [ \omega - \mu ]
# \end{align*}

def add_stuff(a, b):
    result = a + b
    """
    Now we return the result, wee! \[ a = b \]
    Hurray! I'm so excited I can't contain
    my joy to just one or two lines!
    """
    return result
